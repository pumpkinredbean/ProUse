interface:
  display_name: "ProUse"
  short_description: "Use Pro models across your local workspaces."
  default_prompt: "$pro-advisor로 기존 Pro 세션에 현재 쟁점을 검토받고 검증 결과를 작업 상태에 반영해줘."
policy:
  allow_implicit_invocation: true
